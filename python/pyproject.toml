[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "pebr-stats"
version = "0.1.0"
description = "Research and backtesting engine for Pesquisas Eleitorais BR"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
